{
  "name": "@mongez/cache",
  "version": "2.0.0",
  "description": "Framework-agnostic async cache facade with pluggable drivers — localStorage, sessionStorage, IndexedDB, runtime memory, plus encrypted variants. One API, key prefixing, TTL, and a PersistAdapter-compatible shape for @mongez/atom.",
  "main": "src/index.ts",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@mongez/encryption": "^2.0.0"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "happy-dom": "^15.7.0",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  },
  "keywords": [
    "cache",
    "cache-manager",
    "local-storage",
    "session-storage",
    "indexeddb",
    "async-cache",
    "runtime-cache",
    "memory-cache",
    "encryption-local-storage",
    "encrypted-session-storage",
    "browser-cache",
    "browser",
    "ttl",
    "expiration",
    "key-prefix",
    "persist-adapter",
    "storage"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/hassanzohdy/mongez-cache"
  },
  "author": "hassanzohdy",
  "license": "MIT"
}
